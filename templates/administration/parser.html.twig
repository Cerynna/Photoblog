{% extends 'baseAdmin.html.twig' %}

{% block body %}
    <div class="space"></div>
    <div class="album">
        <div class="container">
            <div class="content">
                {% if listDir is not empty %}
                    {{ dump(listDir) }}

                {% else %}
                    Il te faut dabord télécharger un Zip pour utilisé le

                {% endif %}

            </div>
        </div>
    </div>
{% endblock %}


{% block javascript %}

{% endblock %}
