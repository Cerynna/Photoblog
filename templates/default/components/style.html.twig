<style>
    {% for key,background in backgrounds %}
    .bgimg-{{ key+1 }} {
        background: url("{{ vich_uploader_asset(background.photo, 'imageFile') }}");
        background-size: cover;
        background-attachment: fixed;
    }
    {% endfor %}
</style>