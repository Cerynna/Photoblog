<style>
    {% for key,photo in backgrounds %}
    .bgimg-{{ key+1 }} {
        background: url("{{ vich_uploader_asset(photo, 'imageFile') }}");
        background-size: cover;
        background-attachment: fixed;
    }
    {% endfor %}
</style>